<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.androidwearjderobot" >

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".cameraview"
            android:label="AndroidWear.cameraview"
            android:theme="@android:style/Theme.DeviceDefault" />
        <activity
            android:name=".obstaclescreen"
            android:label="@string/title_activity_obstaclescreen" >
        </activity>
        <activity
            android:name=".countingview"
            android:label="@string/title_activity_countingview" >
        </activity>
        <activity
            android:name=".resultactivity"
            android:label="@string/title_activity_resultactivity" >
        </activity>
    </application>

</manifest>
